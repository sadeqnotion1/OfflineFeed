Drop your font files here so app.py can register them at startup:

  Roboto-Regular.ttf, Roboto-Medium.ttf, Roboto-Bold.ttf   (Telegram Desktop UI font)
  Vazirmatn-Regular.ttf, Vazirmatn-Bold.ttf                 (Persian / RTL fallback)

The app still runs without them (Qt falls back to a system sans-serif),
but for pixel-accurate Telegram parity, add Roboto.
